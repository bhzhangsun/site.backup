# SSH known_hosts for deploy targets.
#
# 格式注意（重要！OpenSSH known_hosts 解析规则）:
#   - 默认端口 22: 第一列必须写裸 host（如 "45.134.36.142"），不能带方括号
#   - 非默认端口 NNNN: 第一列必须写 "[host]:NNNN"（带方括号）
#   - ssh-keyscan 默认输出 [host]:port 格式，对默认端口 22 必须用 sed 改写:
#
#   # my-vps (默认端口 22, 去方括号 + 去端口后缀)
#   ssh-keyscan -p 22    -T 8 -t ed25519 45.134.36.142 \
#     | sed -E 's/^\[([^]]+)\]:22$/\1/' >> deploy/known_hosts
#
#   # uk-vps (非默认端口 24743, 保持 ssh-keyscan 原样)
#   ssh-keyscan -p 24743 -T 8 -t ed25519 212.135.42.43 >> deploy/known_hosts
#
# 指纹本身是公开信息（keyscan 即可获得），入仓零安全风险，
# 但能彻底绕开 GitHub Secret 缓存 / 复制粘贴导致的 known_hosts 不一致问题。
45.134.36.142 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICzjgNjYgOy0mkyLRtkPUn/XplgbHr74mKwDWEUrvZMD
[212.135.42.43]:24743 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIImngcTINNpvA4AHoDZeXiwx++J7qGaWbOUJAfljTiNL
